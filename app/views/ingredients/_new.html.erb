<%= form_with model: @ingredient do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit "Add Ingredient", class: "button button--form" %>
<% end %>
